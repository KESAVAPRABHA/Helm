apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "quote-helm.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "quote-helm.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "quote-helm.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.containerPort }}
      imagePullSecrets:
        - name: regsecret
